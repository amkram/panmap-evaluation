# Large per-species inputs — fetched with the rsync command in the README.
# The small samples.tsv + duplicate_groups.tsv beside them ARE committed.
data/**/*.panman
data/**/*.fa
data/**/*.fai
data/tb/clockwork_ref

# Evaluation run artifacts (regenerated by `snakemake`)
work/
results/
.eval_cache/
.snakemake/
**/__pycache__/
*.pyc
loo_exp/
temp_panman/
gmon.out
